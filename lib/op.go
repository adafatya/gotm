package lib

import (
	"turing/lib/mod"
	"turing/lib/log2"
	"turing/lib/add"
)

func Mod(n int, m int)  {
	mod.Mod(n, m)
}

func Log2(n int)  {
	log2.Log2(n)
}

func Add(n int, m int) {
	add.Add(n, m)
}